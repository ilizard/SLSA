name: SLSA Go releaser

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator-goreleaser.yml@v2.0.0
    with:
      go-version: 1.19






